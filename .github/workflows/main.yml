on: [push]

jobs:
  soos:
    permissions:
      security-events: write # for uploading code scanning alert info
    name: SOOS SAST Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Run SOOS DAST analysis testing
        uses: soos-io/soos-dast-github-action@fix/PA-12859 
        with:
          client_id: ${{ secrets.QA_SOOS_CLIENT_ID }}
          api_key: ${{ secrets.QA_SOOS_API_KEY }}
          #scan_mode: "apiscan"
          auth_login_url: "https://demoqa.com/login" 
          target_url: "https://demoqa.com/profile" 
          auth_verification_url: "https://demoqa.com/Account/v1/User/52d9d238-fc85-4f1e-9ce2-50a65397ed29"
          auth_username: "josetest000" 
          auth_password: "J0s3#t3st" 
          project_name: "DAST-GHActions-Auth-Single-Page"
          api_url: "https://qa-api.soos.io/api/"
          verbose: "true"
